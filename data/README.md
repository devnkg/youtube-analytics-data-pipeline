# Sample Data

`sample/` contains lightweight category mappings used for local development.
Regional Kaggle CSV extracts are intentionally ignored because they are large;
download them separately into this directory before running the historical upload
script. Production data belongs in S3 and should not be committed to this repository.