# Tests

Add unit tests for Lambda handlers, Glue transformation rules, and data-quality
checks here. Integration tests should run against isolated AWS resources or mocked
AWS clients rather than production buckets and topics.